# Auth Service Environment Variables
# Copy this file to .env and fill in your Supabase credentials

# Server Configuration
# Port for the Auth Service
PORT=3001

# Host to bind to (0.0.0.0 allows connections from all interfaces)
# Use 0.0.0.0 for device access, localhost for local-only access
HOST=0.0.0.0

# Supabase Configuration
# Your Supabase project URL
# Get this from: https://app.supabase.com/project/_/settings/api
# Format: https://your-project-id.supabase.co
SUPABASE_URL=https://your-project-id.supabase.co

# Supabase Service Role Key (SECRET - Never expose in client code!)
# This key has admin privileges and bypasses Row Level Security (RLS)
# Get this from: https://app.supabase.com/project/_/settings/api
# Look for "service_role" key (NOT the anon key)
# âš ï¸ WARNING: Keep this secret! Only use on backend services.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Instructions:
# 1. Go to https://app.supabase.com
# 2. Select your project
# 3. Go to Settings > API
# 4. Copy the "Project URL" to SUPABASE_URL
# 5. Copy the "service_role" key (not anon key) to SUPABASE_SERVICE_ROLE_KEY
# 6. Save this file as .env (without .example)

# Tenant onboarding (additional companies after the first)
# After your first company exists, POST /api/auth/onboard-company requires header:
#   X-Onboarding-Key: <same value>
# Generate a long random string (min 16 characters).
COMPANY_ONBOARDING_SECRET=